/* If more than a string is needed, simply add the other(s),
   separating them with commas. 
   Remember that the array must always be NULL terminated.
   For example:
   #define MATCHES {"STR1", "STR2", "STR3", NULL} */
#define MATCHES {"RUNNING", NULL}
